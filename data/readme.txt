The constants file have some static data to use in some places on the site.

The filter-options file have the result of fileOptions query, used to populate the options of the form of filter by location.

The step-a file represent search query result with list of medicalCosts.

The step-b file represents the query result of a specific medicalCosts.

The step-b file represents the query result of a specific medicalCosts with more cost details.